{%
  set classes = [
    '<%= tag %>',
    '<%= tag %>-' ~ configuration.provider|clean_class,
    '<%= tag %>-' ~ plugin_id|clean_class,
  ]
%}

<div{{ attributes.addClass(classes) }}>
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>